type: object
properties:
  token:
    description: The API token
    type: string
  type:
    description: The API token type
    type: string
    enum:
      - bearer
  expiresIn:
    description: The API token expiration time
    type: integer
    format: int64
    example: 3600
